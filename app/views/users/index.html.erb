<h1>List of Users</h1>

<ul class="user-list">
  <% @users.each do |user| %>
    <li class="user-item">
      <div class="user-pic"></div>
      <div class="user-info">
        <h2><a href="<%= user_path(user) %>"><%= user.name %></a></h2>
        <p>Number of posts: <%= user.posts.count %></p>
      </div>
    </li>
  <% end %>
</ul>
